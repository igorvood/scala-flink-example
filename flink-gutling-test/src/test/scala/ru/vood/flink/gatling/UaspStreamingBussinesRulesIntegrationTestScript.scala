package ru.vood.flink.gatling

import io.gatling.core.scenario.Simulation

class UaspStreamingBussinesRulesIntegrationTestScript/* extends Simulation*/ {


  /*
    setUp(
      scenarioBusinessRules.inject(atOnceUsers(COUNT_USERS)).protocols(kafkaConf)
    )
  */
}
